import { DisabledWhenLoadingDirective } from './disabled-when-loading.directive';

describe('DisabledWhenLoadingDirective', () => {
  it('should create an instance', () => {
    const directive = new DisabledWhenLoadingDirective();
    expect(directive).toBeTruthy();
  });
});
